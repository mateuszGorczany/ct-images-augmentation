name: mini_gan

training:
  batch_size: 2
  seed: 42
  epochs: 10
  # val_every_step: 200
  # lr_warmup_steps: 2000
  # lr_decay_until_steps: ${.num_steps}
  # lr_decay_factor: 0.001
  # weight_decay: 0.1
  # num_steps: 20000
  num_nodes: 1
  device: "cuda"
  # amp_precision: bfloat16
  # weight_precision: float32
  # enable_mixed_precision: true

model:
  learning_rate: 0.0002
  img_shape: [1, 256, 256, 32]
  latent_dim: 100
  beta1: 0.5
  beta2: 0.999
  device: "cuda"

dataset:
  path: /ravana/d3d_work/micorl/data/ct_images_prostate_32fixed/
  # path: /home/mateusz/Documents/MGR/Thesis/ct-images-augmentation/data/input/ct_images
